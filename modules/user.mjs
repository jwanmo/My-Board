class User {
    constructor(email, pswHash, name, city) {
        this.email = email;
        this.pswHash = pswHash;
        this.name = name;
        this.city = city;
    }
}

export default User;
